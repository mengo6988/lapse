# 09 — Design tokens and app shell

**What to build:** The app looks like lapse and navigates like lapse: the committed ledger × mocha palette and type, the bottom tab bar with its center FAB, and the four routes in place (two of them honest stubs).

**Blocked by:** 01 (Walking skeleton).

**Status:** resolved

- [x] Design tokens for the mocha palette with the urgency accents, Source Serif 4 and IBM Plex Mono, the noise texture, dark-only per `docs/design.md`
- [x] Bottom tab bar with a center FAB; the header is the wordmark plus the magnifier only (the sliders icon was deliberately dropped)
- [x] Home, list, activity, and settings routes exist and navigate; activity and settings are labeled stubs until milestone 5
- [x] `prefers-reduced-motion` collapses motion globally, and touch targets meet the 44px minimum
